## Packages
date-fns | Date formatting for schedules and events
recharts | Dashboard statistics visualization
framer-motion | Page transitions and smooth interactions

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
Auth endpoints: /api/login, /api/register, /api/user, /api/logout
Backend uses cookie-based auth (credentials: "include")
